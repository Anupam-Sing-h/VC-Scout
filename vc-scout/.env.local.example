# Environment Variables

### FIRECRAWL_API_KEY
Your Firecrawl API key for the /api/enrich endpoint. The API key is read **server-side only** and is never exposed to the browser.

Get a free key at https://firecrawl.dev

```
FIRECRAWL_API_KEY=fc-your-api-key-here
```

## Setup
1. Copy this file: `cp .env.local.example .env.local`
2. Replace the placeholder value with your real key
3. Restart the dev server: `npm run dev`

